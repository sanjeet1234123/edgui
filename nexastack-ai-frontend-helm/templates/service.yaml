apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexastack-ai-frontend-helm.fullname" . }}
  labels:
    {{- include "nexastack-ai-frontend-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexastack-ai-frontend-helm.selectorLabels" . | nindent 4 }}
